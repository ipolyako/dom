<UserControl x:Class="FastDOM.App.Views.PositionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             Background="{StaticResource PanelBrush}">
    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="90"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Symbol:" Foreground="{StaticResource SecondaryForeBrush}" FontSize="11"/>
            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Symbol}" FontWeight="Bold" FontSize="13"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Position:" Foreground="{StaticResource SecondaryForeBrush}" FontSize="11"/>
            <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal">
                <TextBlock Text="{Binding Side}" FontWeight="Bold" FontSize="13"
                           Foreground="{Binding Side, Converter={StaticResource SideBrushConverter}}"/>
                <TextBlock Text=" " FontSize="13"/>
                <TextBlock Text="{Binding Quantity}" FontSize="13"/>
            </StackPanel>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Avg Cost:" Foreground="{StaticResource SecondaryForeBrush}" FontSize="11"/>
            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding AverageCost, StringFormat=F2}" FontSize="12"/>

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Unr. P/L:" Foreground="{StaticResource SecondaryForeBrush}" FontSize="11"/>
            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding PnLDisplay}" FontSize="13" FontWeight="Bold"
                       Foreground="{Binding UnrealizedPnL, Converter={StaticResource PnLBrushConverter}}"/>

            <TextBlock Grid.Row="4" Grid.Column="0" Text="BP:" Foreground="{StaticResource SecondaryForeBrush}" FontSize="11"/>
            <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding BuyingPower, StringFormat='${0:N0}'}" FontSize="11"/>
        </Grid>
    </Border>
</UserControl>
